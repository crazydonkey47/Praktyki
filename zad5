package com.company;

public class Main {

    public static void main(String[] args) {
        System.out.println(gcd(25, 5));
    }

    public static int gcd (int x, int y) {
        if (y == 0)
            return x;
        else
            return gcd (y, x % y);
    }

}
